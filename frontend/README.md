# Frontend (client)

This folder will host the primary frontend app (Next.js / Remix / Vite). Currently the repository contains `frontend-next/` (legacy Next.js) and `src/` (TanStack Start). During restructure we will choose the canonical frontend and move it here.

Next steps:
- Move `frontend-next/` or `src/` into this directory (dry-run first).
- Update CI and Docker build to point to this path.
